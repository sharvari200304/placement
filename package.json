{
  "name": "placement-cell-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Placement Cell Portal with React, Express, MongoDB, JWT, and Tailwind CSS.",
  "scripts": {
    "db": "C:\\Progra~1\\MongoDB\\Server\\8.0\\bin\\mongod.exe --dbpath .mongodb-data --logpath .mongodb-log\\mongod.log --logappend --bind_ip 127.0.0.1 --port 27017",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
